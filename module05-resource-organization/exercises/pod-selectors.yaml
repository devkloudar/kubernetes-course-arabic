apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  selector:
    app: nginx   # يربط الخدمة بالـ Pods التي لديها label app=nginx
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
