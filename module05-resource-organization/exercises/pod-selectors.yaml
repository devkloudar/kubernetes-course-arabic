apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  type: NodePort
  selector:
    app: nginx   # يربط الخدمة بالـ Pods التي لديها label app=nginx
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
