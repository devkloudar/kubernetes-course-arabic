apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-app2
  labels:
    app: web-app2
spec:
  replicas: 2
  selector:
    matchLabels:
      app: web-app2
  template:
    metadata:
      labels:
        app: web-app2
    spec:
      containers:
      - name: nginx
        image: nginx:alpine
        ports:
        - containerPort: 80
        volumeMounts:
        - name: html-content
          mountPath: /usr/share/nginx/html
      volumes:
      - name: html-content
        configMap:
          name: app2-content
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: app2-content
data:
  index.html: |
    <!DOCTYPE html>
    <html>
    <head>
        <title> The Second App </title>
    </head>
    <body>
        <h1> Welcome to the Second App (App2) </h1>
        <p> This app works with Ingress Minikube </p>
    </body>
    </html>
---
apiVersion: v1
kind: Service
metadata:
  name: web-app2-service
spec:
  selector:
    app: web-app2
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
